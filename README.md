# Hestreg

Hand gesture recognition system that allows you to create your own Recognition 
models and generate your custom recognition system.

*warning*: when choosing the model name don't use - in the name as it is 
required for `model-detele.sh` script to differentiate between commnad and 
model name

> Note: Cleaned git history
