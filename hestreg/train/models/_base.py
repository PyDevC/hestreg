import torch.nn as nn
from torch.nn import functional as F

class base_cnn(nn.Module):
    def __init__(self):
        super().__init__()
        # starting building layers can we add any other functionality
        # if you want to create any other functionality then its better to create a new class for that
        # lets call this different types of neural network architectures
        # you can add initial weights to the neural network
    def forward(self):
        pass
